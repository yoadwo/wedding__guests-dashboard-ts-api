export interface guestEM {
    firstName: string,
    lastName: string,
    phoneNumber: string,
    phoneNumberHash: string,
    status: number
}

export interface guestRM {
    firstName: string,
    phoneNumber: string,
    phoneNumberHash: string,
}